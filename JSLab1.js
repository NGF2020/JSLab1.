function isInteger(num) {
    return num % 1 === 0;
}

let uInput = prompt("");
let number = Number(uInput);

if (isInteger(number)) {
    alert("t");
} else {
    alert("f");
}

function findNums(a, b) {
    let Nums = [];

    function isNums(n) {
        let isNums = true;
        if (n < 2) return false;
    }
}